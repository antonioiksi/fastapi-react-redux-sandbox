version: '3.7'
services:

  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    command: python main.py
    # command: uvicorn app.api:app --reload --host 0.0.0.0 --port 8080
    # tty: true
    ports:
      - 8502:8080
    volumes:
      - ./backend:/app/:cached
    #   - ./.docker/.ipython:/root/.ipython:cached
    environment:
      PYTHONPATH: .
      DATABASE_URL: 'postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}'
    depends_on:
      - "postgres"

  postgres:
    image: postgres:12
    restart: always
    env_file:
      - ./docker-compose.env
    # environment:
    #   # POSTGRES_USER: cookiecutter_postgres_user
    #   POSTGRES_PASSWORD: cookiecutter_postgres_root_password
    #   # 
    #   DB_USER: cookiecutter_postgres_user
    #   DB_PASSWORD: cookiecutter_postgres_password
    #   DB_NAME: cookiecutter_postgres_database
    # ports:
    #   - '8501:5432'
    volumes:
      # - ./backend/scripts:/www/scripts
      - ./backend/scripts/create-db:/docker-entrypoint-initdb.d # load data after postgres started

  # nginx:
  #   image: nginx:1.17
  #   volumes:
  #     # - ./nginx/conf.d:/etc/nginx/conf.d
  #     - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf
  #   ports:
  #     - 8500:80
  #   depends_on:
  #     - backend

